import { SolarDatePipe } from './solar-date.pipe';

describe('SolarDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SolarDatePipe();
    expect(pipe).toBeTruthy();
  });
});
